function noBoringZeros(n) {
  var arr = n.toString;
  arr.split;
  for ( var i = (arr.length - 1); i >= 0; i--) {
    if ( arr[i] == 0) {
      arr.pop();
    };
  n = arr.join;
  n = parseInt(n);
  }
  console.log(n);
}